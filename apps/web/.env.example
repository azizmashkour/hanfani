# Web app port (default: 3004 to avoid conflicts with 3000–3003)
PORT=3004

# API base URL for the status page and API calls
# Use 8001 if port 8000 is already in use
NEXT_PUBLIC_API_URL=http://localhost:8001
